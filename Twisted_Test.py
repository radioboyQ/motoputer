"""
* Pull GPS data from <HOST> and <PORT>

* Update LEDs based on current GPS data

* Log current GPS position to SQLite3 once a minute

* Check if wireless network is connected

** If network listed in config is connected, upload data to remote configured SFTP server

* If WEP detected, update colors on buttons
"""

